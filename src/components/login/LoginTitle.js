import React from 'react';
import '../../styles/login-styles.css';

var LoginTitle = React.createClass({
  render: function() {
    return (
      <div className="title">
        <a className="title">Serv</a><a className="titleR">r</a>
      </div>
    );
  }
});

export default LoginTitle;
